# Sprint-13-Project

The objective of this project is to build a machine learning model that predicts rougher concentrate recovery and final concentrate recovery in a gold processing operation within the mining industry.

## Requirements
Install the required dependencies with:

pip install -r requirements.txt

